#include "main.h"
/**
 * add - add two integers
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
